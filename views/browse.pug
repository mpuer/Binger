extends layout.pug

block content
    div(class="column browse-content")
        h2= `TV Show`
        div
            ul
                each show in tvShows
                    li #{show.images}
                    li #{show.name}
                    li #{show.genre}
                    li #{show.rating}
                    li #{show.synopsis}








        button(type='Logout')
