extends layout.pug

block content
    div(class="column user-content")
        div(class="column channel-content")
        h2 Channels
            ul
                each channel in channels
                    li
                        div
                            p #{channel.title}

