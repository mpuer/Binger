extends navbar
    form(method="GET" action="/sign-up")
        label(for="username") Username:
        input(type="string" id="username" name="username")
        label(for="password") Password:
        input(type="string" id="password" name="password")
